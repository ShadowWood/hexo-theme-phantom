
<%
var tags = [];
item.tags.forEach(function(tag){
    tags.push('<a href="' + config.root + tag.path + '" class="button small tag">' + tag.name + '</a>');
});
%>

<div class="tags">
    <%- tags.join(' ') %>
</div>

