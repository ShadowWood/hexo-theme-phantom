
<% if(theme.show_dates) { %>
    <h1 class="title"><%- item.title || theme.default_post_title || "Untitled" %></h1>
    <div class="meta">
        <%= item.date.format(config.date_format) %>
    </div>
<% } else { %>
    <h1><%- item.title || theme.default_post_title || "Untitled" %></h1>
<% } %>

<!-- Tags -->
<%- partial('article-tags') %>

<% if(item.cover_detail || item.img_url || theme.default_cover_detail) { %>
    <span class="image main"><img src="<%- item.cover_detail || item.img_url || theme.default_cover_detail %>" alt="" /></span>
<% } %>

<!-- Gallery -->
<% if (item.photos && item.photos.length){ %>
    <%- partial('gallery') %>
<% } %>

<!-- Content -->
<%- item.content %>

<!-- Comments -->
<div>
    <%- partial("_partial/comments.ejs") %>
</div>

